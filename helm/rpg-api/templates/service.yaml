apiVersion: v1
kind: Service
metadata:
  name: rpg-api
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: rpg-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP
